import React from "react";
import { Link } from "react-router-dom";

const Logout = () => {
  return (
    <div>
    <h5 style={{textAlign:"center"}}>Logout</h5>
    </div>
  );
};

export default Logout;
